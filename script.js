window.addEventListener("DOMContentLoaded", function () {
  $('#flipbook').turn({
    width: '100%',
    height: '100%',
    autoCenter: true,
    gradients: true,
    elevation: 50
  });
});
